# Pre-defined scripts

Add Illustrator ExtendScript files with a `.jsx` or `.js` extension to this folder. They will appear automatically in the extension's **Scripts** tab and run in Illustrator when selected.
